# OpenLoops

This is the `stable-board-2015` branch which used to be master but at the
end of 2015, I create this branch so I could start taking master in a new
direction (columns and queues).

This is really for if I want to continue working on the old direction at
any point.
